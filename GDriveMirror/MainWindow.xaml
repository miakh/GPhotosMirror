<Window x:Class="GDriveMirror.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GoogleDriveUploader"
        mc:Ignorable="d"
        Title="Google Drive Uploader" Height="450" Width="800"
        d:DataContext="{d:DesignInstance local:MainViewModel}"
        >
    <Grid>
        <Button Content="Change Path" HorizontalAlignment="Left" Margin="120,46,0,0" VerticalAlignment="Top" Command="{Binding ChangePathCommand}"/>
        <TextBlock HorizontalAlignment="Left" Margin="230,48,0,0" Text="{Binding Root}" TextWrapping="Wrap" VerticalAlignment="Top"/>
        <TextBlock HorizontalAlignment="Right" Margin="0,12,12,0" Text="{Binding UserName}" TextWrapping="Wrap" VerticalAlignment="Top"/>

    </Grid>
</Window>
